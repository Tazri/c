/**
 * 
 * there are four type of unary operator in c
 * think we have a number which name is 'number'
 * number++ => post increment
 * ++number => pre increment
 * number-- => post dcrement
 * --number => pre dcrement
*/
